# GitHub Pages demo site

This directory is the static GitHub Pages front-end for the portfolio project.

It intentionally does not call the FastAPI service because GitHub Pages is static hosting. Instead, `app.js` provides a browser-only scoring simulator using synthetic example companies and explainable heuristic scoring.

Publish path: `site/`
Entry file: `site/index.html`
